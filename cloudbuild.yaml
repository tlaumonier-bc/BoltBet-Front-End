substitutions:
  _REGION: europe-west2
  _REPO: vibe-apps
  _SERVICE: boltbet-frontend-web
  _IMAGE: boltbet-frontend
  _RUNTIME_SA: vibe-app-runtime@blockchain-gemini.iam.gserviceaccount.com
  # Next.js public env is baked in at build time. Override these with the
  # deployed backend Cloud Run URL, for example:
  # --substitutions=_NEXT_PUBLIC_API_URL=https://...,\
  # _NEXT_PUBLIC_WS_URL=wss://.../ws/lightning/
  _NEXT_PUBLIC_API_URL: https://api.lightningmapsgame.com
  _NEXT_PUBLIC_WS_URL: wss://api.lightningmapsgame.com/ws/lightning/
  _NEXT_PUBLIC_GAME_SERVER: "1"
  _NEXT_PUBLIC_OWM_API_KEY: ""
  _NEXT_PUBLIC_FIREBASE_API_KEY: AIzaSyBnwj0QnamOhspYGG_4tUQRBOTD0mgMtCo
  _NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: blockchain-gemini.firebaseapp.com
  _NEXT_PUBLIC_FIREBASE_PROJECT_ID: blockchain-gemini
  _NEXT_PUBLIC_FIREBASE_APP_ID: 1:374738273879:web:d064521c6b1edc279d8512
  _NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "374738273879"
  _NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: blockchain-gemini.firebasestorage.app

steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: bash
    args:
      - -ceu
      - |
        test -n "${_NEXT_PUBLIC_API_URL}" || { echo "_NEXT_PUBLIC_API_URL is required"; exit 1; }
        test -n "${_NEXT_PUBLIC_WS_URL}" || { echo "_NEXT_PUBLIC_WS_URL is required"; exit 1; }

  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}:$COMMIT_SHA
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}:latest
      - --build-arg
      - NEXT_PUBLIC_API_URL=${_NEXT_PUBLIC_API_URL}
      - --build-arg
      - NEXT_PUBLIC_WS_URL=${_NEXT_PUBLIC_WS_URL}
      - --build-arg
      - NEXT_PUBLIC_GAME_SERVER=${_NEXT_PUBLIC_GAME_SERVER}
      - --build-arg
      - NEXT_PUBLIC_OWM_API_KEY=${_NEXT_PUBLIC_OWM_API_KEY}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_API_KEY=${_NEXT_PUBLIC_FIREBASE_API_KEY}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${_NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_PROJECT_ID=${_NEXT_PUBLIC_FIREBASE_PROJECT_ID}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_APP_ID=${_NEXT_PUBLIC_FIREBASE_APP_ID}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${_NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
      - --build-arg
      - NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${_NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
      - -f
      - Dockerfile
      - .

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}
      - --image=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}:$COMMIT_SHA
      - --region=${_REGION}
      - --platform=managed
      - --service-account=${_RUNTIME_SA}
      - --allow-unauthenticated
      - --port=3000
      - --memory=1Gi
      - --cpu=1
      - --min-instances=0
      - --max-instances=2

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}:$COMMIT_SHA
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_IMAGE}:latest

options:
  logging: CLOUD_LOGGING_ONLY